name = "getaway"
version = "0.4.0"
description = "getaway guardrails: first-run onboarding nudge (auto-fill from Gmail and airline logins) and stop-time reflection folding session learnings into ~/.getaway preferences, the active trip plan, or the skill docs"
hooks = "hooks"
