{"v":1,"lamport":1,"ops":[{"kind":"create_doc","nonce":"21e739e2d2f99d869757c661e73b03b1","title":"Phase 4a digest: loyalty registry + travel_instruments (v2)","body":"# Phase 4a consolidated digest — B1–B3 + cutover + Wyndham (2026-07-13)\n\nAll values verified via web 2026-07-13; sources per row in transfer_partners.json/points_pricing.json notes.\n\n## (1) Transfer-partner rows added (bank→hotel; ratio = bank:hotel)\n- amex→hilton 1:2; amex→marriott 1:1; amex→choice 1:1. Amex is NOT a partner of wyndham/hyatt/ihg.\n- chase→hyatt 1:1 (note: Sapphire Reserve stays 1:1; Sapphire Preferred + Ink Business Preferred are 4:3 for applications on/after 2026-06-15, existing SP holders convert 1:1→4:3 on 2026-10-01); chase→ihg 1:1; chase→marriott 1:1; chase→wyndham 1:1 (Chase's first new partner since 2021, added 2026-02-25). All chase rows: 1,000-pt increments, transfer-eligible card required.\n- citi→choice 1:1.5 (devalued from 1:2 on 2026-04-19; premium ThankYou card required); citi→wyndham 1:1 (same card requirement).\n- capitalone→choice 1:1; capitalone→wyndham 1:1.\n- Reachability: hyatt←chase; hilton←amex; marriott←amex,chase; ihg←chase; choice←amex,citi,capitalone; wyndham←chase,citi,capitalone.\n- Excluded deliberately: Bilt Rewards (no bank entity in the registry).\n\n## (2) points_pricing rows (buy¢ / sale¢ / annual cap — cadence)\nhyatt 2.6/2.1/55k (rare, shallow); hilton 1.0/0.5/160k→240k promo (near-constant 100% bonuses); marriott 1.25/0.8/150k (frequent ~40% bonus); ihg 1.0/0.5/150k→200k promo (very frequent 80–100%); choice 1.1/0.74/180k (~monthly 35–40%); wyndham 1.3/0.72/100k→200k promo (near-continuous 70–100% targeted). Test-enforced invariant: every program has a points_pricing row and sells_points agrees between files (34 programs).\n\n## (3) travel_instruments schema as implemented\n`credits`→`travel_instruments`; `credit_*`→`instrument_*`; id server-generated (uuid hex[:8]), caller-supplied id rejected; dispatch on `type`.\n- monetary_credit: {id, type, issuer(free-text), amount(\u003e0), currency, expires[, note]}\n- hotel_night_certificate: {id, type, program(MUST be hotel registry slug), nights(int≥1), cap: {type:\"points\",points≥1}|{type:\"category\",category}|{type:\"anytime\"}, expires[, note]}\n- companion_fare: {id, type, issuer, expires[, note]} — NO amount/currency.\nWrite-boundary rejections (UsageError, exit 64): unknown/missing type; missing/extra keys per variant; cert program not a hotel; nights\u003c1; bad cap; amount≤0/non-number; caller id. CLI: `prefs instrument-add` (one JSON object on stdin), `instrument-list [--expiring-within 90d]`, `instrument-remove \u003cid\u003e`.\nCUTOVER: any pre-v2 doc (has `credits` or lacks `travel_instruments`) rejected on EVERY read/write path with StateConflictError (exit 3): \"preferences use a pre-v2 shape (the credits list became travel_instruments); delete \u003cprefs_path\u003e and re-run getaway onboarding\". No migration, no alias.\n\n## (4) Registry command surfaces\n- `registry programs` gains `--kind airline|hotel`, `--rooms-aero` (composable with `--domains`; `--domains` output shape unchanged — Gmail-sender source).\n- NEW `registry hosts [--kind airline|hotel|bank] [--gather-auth cookie|token|device_wall]` → [{slug, kind, gather_auth, hosts:[…]}] — the single seam for the browser read's host list + auth routing (38 rows: 34 programs + 4 banks; banks carry [domain, dashboard_host]).\n- `registry banks` output includes gather_auth.\n- Helpers: registry.programs_by_kind(kind), registry.gather_hosts(), registry.GATHER_AUTH_CLASSES.\n- gather_auth classes (test-asserted): token = delta, american, united, jetblue, aeroplan, qatar, singapore + capitalone(bank); device_wall = amex(bank); cookie = everything else incl. all 6 hotels, chase, citi.\n\n## (5) Phase-8 doc deltas (complete list)\n**gather.md \"Program and bank registries\":** keep `registry programs --domains` (Gmail from:) + `registry banks`; ADD `registry hosts` as the browser-read host list with gather_auth; hotel hosts join via --kind hotel; state the two projections' distinct purposes.\n**gather.md \"Browser read\":** route by gather_auth — cookie → seeded session (today's path); token (delta/american/united/jetblue/aeroplan/qatar/singapore/capitalone) + device_wall (amex) → live Arc CDP attach. DELETE the v1 \"lands logged-out despite fresh cookies: skip the host\" line and its re-login advice (v1 mislabeled structural failures). Failure messaging distinguishes STRUCTURAL vs TRANSIENT. Consolidated retry pass runs off an explicit per-host ledger (capitalone silently dropped in v1). --reason spans airline+hotel+bank hosts.\n**gather.md \"Credits mining\":** `credit-add` flags → `prefs instrument-add` with one JSON object on stdin; drop the kind vocabulary (voucher/credit/certificate/companion) for the tagged-union types; id CLI-generated; hotel domains join the sender list via --kind hotel --domains; add hotel subject terms (free-night award/certificate, anniversary night, \"Category 1-4\", elite-night credit).\n**onboard + refresh SKILL.md:** one hotels balance field from `registry programs --kind hotel` (6 hotels) into the single balances.programs map; hotel elite tiers in statuses (Globalist, Diamond, Titanium/Ambassador, IHG Diamond, Wyndham Diamond); gather_auth routing + per-host ledger mirrored; travel_instruments form section replaces credits(kind); onboarding asks for missing expiry, refresh keeps on-file; refresh cert-mining captures hotel free-night certs.\n**planning.md + SKILL.md CLI map:** one balances.programs map narrative (no is_hotel()); hotels as transfer targets; credits→travel_instruments everywhere; CLI map adds registry hosts / --kind / --rooms-aero / instrument-*, removes credit-*.\n\n## Open verification items\n- All 6 hotel sites classified gather_auth: cookie by default — no evidence of token auth; reclassify per-slug (one-field data edit) if a live refresh lands logged-out with fresh cookies.\n- Wyndham's rooms.aero `source` slug recorded as \"wyndham\", observed-pending-confirmation by a live capture (Phase 6 stays-ingest concern).","when":"Writing the getaway v2 docs sweep (gather.md, onboard/refresh SKILL.md, planning.md, SKILL.md CLI map) or reviewing the Phase 4a registry/instruments diff — this is the implementing agent's consolidated digest with verified ratios, schema shapes, and the complete doc-delta list.","tags":["getaway-v2","phase-4a"],"anchors":[{"kind":"path","value":"cli/getaway/data"},{"kind":"path","value":"cli/getaway/prefs.py"},{"kind":"path","value":"cli/getaway/registry.py"},{"kind":"path","value":"skills/refresh"},{"kind":"path","value":"skills/onboard"}]}]}